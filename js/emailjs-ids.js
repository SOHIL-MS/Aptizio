/**
 * Paste your IDs from https://dashboard.emailjs.com/admin
 * → Email Services (Service ID) and Email Templates (Template ID).
 *
 * Leave both empty to use mailto (only works in a normal browser tab, not editor previews).
 */
window.APTIZIO_EMAILJS = {
  serviceId: "",
  templateId: ""
};
